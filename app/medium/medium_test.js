'use strict';

describe('myApp.medium module', function() {

  beforeEach(module('myApp.medium'));

  describe('medium controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var mediumCtrl = $controller('MediumCtrl');
      expect(mediumCtrl).toBeDefined();
    }));

  });
});